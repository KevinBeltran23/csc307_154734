{
  "name": "csc-307-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"cd packages/react-frontend && npm run dev\" \"cd packages/express-backend && npm run dev\""
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "mongoose": "^8.3.2",
    "prettier": "^3.2.5",
    "router-dom": "^2.2.11"
  }
}
